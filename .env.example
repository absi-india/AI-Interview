# ─── LOCAL DEV ────────────────────────────────────────────────────────────────
# Copy to .env and fill in values.

# ─── FIREBASE ─────────────────────────────────────────────────────────────────
# Client SDK — from Firebase Console → Project Settings → Your apps → Web app
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Admin SDK — from Firebase Console → Project Settings → Service accounts → Generate new private key
FIREBASE_PROJECT_ID=your-project
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxx@your-project.iam.gserviceaccount.com
# Paste the entire private key (including -----BEGIN/END----- lines) as one line with \n
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# App
# Use a URL candidates can open. For local LAN testing, use your machine's
# network URL, for example http://192.168.1.9:3000, not localhost.
APP_DOMAIN=http://localhost:3000
AUTH_SECRET=change-me-to-a-strong-random-string-at-least-32-chars
# Local dev: leave NEXTAUTH_URL unset so Auth.js uses the current host
# (works with both localhost and LAN URLs like http://192.168.1.9:3000).
# NEXTAUTH_URL=http://localhost:3000
# Optional for local LAN testing with production-mode server (`next start`):
# AUTH_TRUST_HOST=true

# ─── DATABASE ─────────────────────────────────────────────────────────────────
# Local dev: SQLite file
DATABASE_URL="file:./dev.db"
DATABASE_AUTH_TOKEN=   # leave empty for local file

# Production (Turso — free): https://app.turso.tech
# DATABASE_URL="libsql://<db-name>.turso.io"
# DATABASE_AUTH_TOKEN="<token from turso auth token>"

# ─── REDIS ────────────────────────────────────────────────────────────────────
# Local dev
REDIS_URL=redis://localhost:6379

# Production (Upstash — free): https://console.upstash.com
# REDIS_URL="rediss://default:<password>@<host>.upstash.io:6379"

# ─── CRON (Vercel cron security) ──────────────────────────────────────────────
# Generate with: openssl rand -hex 32
CRON_SECRET=change-me-to-a-random-secret

# ─── OBJECT STORAGE (optional — videos / resumes) ────────────────────────────
# Local dev: MinIO
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_RECORDINGS=recordings
MINIO_BUCKET_RESUMES=resumes

# Production: Cloudflare R2 (free 10 GB/month) — S3-compatible
# MINIO_ENDPOINT=<account-id>.r2.cloudflarestorage.com
# MINIO_PORT=443
# MINIO_USE_SSL=true
# MINIO_ACCESS_KEY=<r2-access-key-id>
# MINIO_SECRET_KEY=<r2-secret-access-key>
# MINIO_BUCKET_RECORDINGS=recordings
# MINIO_BUCKET_RESUMES=resumes

# ─── AI ───────────────────────────────────────────────────────────────────────
# Gemini API (FREE tier) — https://aistudio.google.com/app/apikey
# Free: 15 req/min, 1M tokens/day on gemini-1.5-flash
GEMINI_API_KEY=AIza...

# ─── SMTP EMAIL (optional) ────────────────────────────────────────────────────
# Free option: Resend (https://resend.com) — 3,000 emails/month
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=465
# SMTP_USER=resend
# SMTP_PASS=<resend-api-key>
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
# Optional: set to true for SSL/TLS (usually required for port 465)
SMTP_SECURE=false
SMTP_USER=noreply@yourdomain.com
SMTP_PASS=yourpassword
SMTP_FROM="Interview Portal <noreply@yourdomain.com>"

# ─── PLATFORM ─────────────────────────────────────────────────────────────────
COMPANY_NAME="Your Company Name"

# ─── SEED (first-run admin account) ──────────────────────────────────────────
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-in-production
